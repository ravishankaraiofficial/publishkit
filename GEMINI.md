# Project Instructions - Google Antigravity Files (PublishKit)

## Project Environment
- **Root Directory:** `D:\Project\Project 01\Google Antigravity Files` (Updated May 11, 2026)

## Development Workflow
- **Frontend:** Run `npm run dev` in the root directory.
- **Backend (Cloud Functions):** Run `npm run build:watch` in the `functions` directory.
- **Firebase:** Uses the `gen-lang-client-0079285803` project.

## Conventions
- Use relative paths in all configuration files to maintain portability.
- Gemini API keys must stay in Firebase Secret Manager, never in `.env`.
